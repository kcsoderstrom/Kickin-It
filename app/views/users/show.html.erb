<h1><%= @user %>'s page</h1>

<% goals_list = ( @user == current_user ? @user.goals : @user.public_goals ) %>

<% goals_list.each do |goal| %>
  <%= goal %>
<% end %>

New Goal

<%= render partial: 'goals/new', locals: { goal: @goal } %>